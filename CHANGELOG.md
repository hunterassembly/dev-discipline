# Changelog

## Unreleased

- Added portable `scripts/docs-list.sh` for docs front-matter validation.
- Added optional `scripts/committer` helper for explicit-file commits.
- Added reusable slash-command docs (`/reconcile`, `/handoff`, `/pickup`).
- Added docs structure/front-matter conventions for project-agnostic onboarding.
- Added local `doc-gardener` workflow for docs/plan drift scanning and score snapshots.
- Added `docs/plans/{active,completed}` scaffolding and plan template.
- Added `docs/QUALITY_SCORE.md` scoreboard artifact for local quality tracking.
