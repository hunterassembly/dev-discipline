# Evals

Harness-engineering projects keep eval assets near the code they protect.

- `cases/`: representative and regression inputs
- `rubrics/`: grading definitions and pass criteria
- `runs/`: run summaries and regression notes

Start with a small but meaningful set of cases and grow as behavior evolves.
